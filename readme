Before running all the modules, Make sure to initialize the environment variables in your system.
We have bugs in NotificationService module, especially compatibality issues. Debug yourelf.
We have 1 bug in Payment Service i.e after the successful transaction while updating the "pending" status to "booked" it isn't changing the status in booking module.
